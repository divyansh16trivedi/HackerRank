Today, we're learning and practicing an algorithmic concept called Recursion.

CODE :

def factorial(n):
    if n<=1:
        return 1
    else :
        return n * factorial(n-1)

n = int(input())
print(factorial(n))
